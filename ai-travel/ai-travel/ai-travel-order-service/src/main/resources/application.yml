server:
  port: 8084

spring:
  application:
    name: ai-travel-order-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

logging:
  level:
    com.ai.travel.order: INFO