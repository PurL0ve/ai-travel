server:
  port: 8083

spring:
  application:
    name: ai-travel-plan-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

logging:
  level:
    com.ai.travel.plan: INFO
